The `fiatlux` script is one of the first I run whenever I need to work on a new computer running ubuntu.

It installs everything I use on a regular basis: languages, applications, and shell utilities.
